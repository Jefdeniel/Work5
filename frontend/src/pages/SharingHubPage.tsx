import useSetTitle from '../hooks/setTitle';

const SharingHubPage = () => {
  useSetTitle('Sharing Hub');
  return (
    <div>
      <p>Sharing Hub Page</p>
    </div>
  );
};

export default SharingHubPage;
