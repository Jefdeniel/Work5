import { Row } from 'react-bootstrap';
import { Colors } from '../../../../@types/Colors';

const ProfilePageHeader = () => {
  return (
    <Row className="--br-lg" style={{ backgroundColor: Colors.Primary200 }}>
      profile header
    </Row>
  );
};

export default ProfilePageHeader;
